<form method="post" action="{{ path('enderecamento_postal_delete', {'id': enderecamento_postal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enderecamento_postal.id) }}">
    <button class="btn">Delete</button>
</form>
